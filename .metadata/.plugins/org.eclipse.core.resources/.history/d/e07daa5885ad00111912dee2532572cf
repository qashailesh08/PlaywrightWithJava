package test;

import org.testng.annotations.Test;

import base.BaseTest;

public class FirstTest extends BaseTest {

	@Test(enabled = true)
	public void main() {

		page.navigate("https://www.flipkart.com/");

		System.out.println(page.title());

		page.locator("//span[@class='b3wTlE']").click();
		webUtil.flushReport();
		
		/*
		 * Locator searchBox = page.locator("(//input[@name='q'])[1]");
		 * 
		 * // searchBox.clear(); searchBox.fill("Shoes for men");
		 * 
		 * page.locator("//a[contains(@href,'shoes+for+men&sid=osp%2Ccil&as')]").click()
		 * ;
		 * 
		 * // list of price of all product are displayed List<Locator> lisPrice =
		 * page.locator("//div[@class='hZ3P6w']").all();
		 * 
		 * // List of percentage of all product are displayed
		 * 
		 * List<Locator> listPerce = page.locator("//div[@class='HQe8jr']/span").all();
		 * 
		 * for (int i = 0; i < lisPrice.size(); i++) {
		 * 
		 * Locator locPrice = lisPrice.get(i);
		 * 
		 * System.out.print(locPrice.innerText());
		 * 
		 * browser.close();
		 * 
		 * 
		 * }
		 */

	}

}
